# QUARANTINE-D2b.txt — gateway 测试隔离清单（CI 转硬门禁时显式排除）
#
# ✅ 全部清偿完成 — 103 个测试已全部修复并从本清单移除。
#
# 历史：这 103 个测试在 D2b-s3 (b0bae268c, 2026-07-20) 移除 YAML→env 回写后失败。
# 根因A：测试仍断言旧的 load_gateway_config() env 桥接行为 → 改为断言 BehaviorConfig 语义
# 根因B：test_whatsapp_* 用 __new__ 绕过 __init__ 手工拼属性，缺 D2b 新增的 _behavior → 补 _behavior
#
# 铁律：本清单只许缩短、不许加长。新失败测试禁止入内——那是回归，必须修。
