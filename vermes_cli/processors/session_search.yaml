content: When the user references something from a past conversation or you suspect relevant cross-session context exists, use session_search to recall it before asking them to repeat themselves.
description: 会话搜索引导
name: session_search
order: 50
processor_type: prompt
replaceable: true
triggers:
  tools:
  - session_search
  type: tool_present
version: 1.0.0
governance:
  risk_tier: L1
  replaceable: true
  mutable_by_aegis: true
  rollback: enabled
  critic_guarded: false
  hash: auto
