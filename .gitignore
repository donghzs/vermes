# ── Python ──
.DS_Store
*.pyc*
__pycache__/
/venv/
.venv/
.pytest_cache/

# ── Env / Secrets ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
/export*
*.ppk
*.pem
privvy*
cli-config.yaml

# ── Build artifacts ──
build/*
!build/installer.nsh
dist/
dist-electron/
dist-modules/*.tar.gz
*.dmg
*.zip
vermes-onefile.spec
*.egg-info/
hermes_agent.egg-info/

# ── Frontend ──
node_modules
node_modules/

frontend/dist/
frontend/coverage/
vermes_cli/web_dist.bak.v1/

# ── Logs / Temp / Data ──
logs/
data/
tmp/
temp_vision_images/
.release_notes.md

# ── External app workspace (DuMate) ──
.dumate/

# ── 前端构建产物（每次 build 变，gitHash 仅调试用） ──
vermes_cli/web_dist/frontend-build.json

# ── Test data ──
tests/quick_test_dataset.jsonl
tests/sample_dataset.jsonl
test_studio_*.py

# ── Legacy / unused dirs (defensive: keep in case of re-creation) ──
web/
hermes-*/*
browser-use/
agent-browser/
mini-swe-agent/
wandb/
testlogs
images/
ignored/
examples/
source-data/
models-dev-upstream/
.worktrees/
environments/benchmarks/evals/
docs/superpowers/

# ── Nix ──
.direnv/
.nix-stamps/
result

# ── Legacy run scripts (no longer in repo) ──
run_datagen_kimik2-thinking.sh
run_datagen_megascience_glm4-6.sh
run_datagen_sonnet.sh

# ── Old web/ assets (synced at build time, not source) ──
web/public/fonts/
web/public/ds-assets/

# ── Skills Hub state ──
skills/.hub/

# ── Reports / Archive (sensitive info risk) ──
archive/
website/static/api/skills-index.json

# ── Legacy Hermes paths (renamed to vermes_cli/) ──
hermes_cli/tui_dist/*
hermes_cli/scripts/

# ── mfgcad runtime artifacts ──
vermes_cli/mfgcad/**/*.bak_gate*
vermes_cli/mfgcad/**/__pycache__/

# ── IDE ──
.vscode/

# ── 前端构建临时产物 ──
# vite 加载 vite.config.js 时会生成带时间戳的副本（vite.config.js.timestamp-*.mjs），
# 每次 npm run build 都可能新增一个。此前 .gitignore 没覆盖 → 以 untracked 形式留在工作树，
# 极易被 `git add -A` 误提交。此处显式忽略。
frontend/vite.config.js.timestamp-*.mjs

# agent workbench traces (not product)
.workbuddy/

# ── 巡检/闸门脚本的机器产物（每次跑都变，不入库）──
# 原则：脚本产物不入库，只有「需要人工裁決的结论」才写进仓库。
# 例外（必须入库，勿忽略）：reports/s2/gold/** —— S2 注入等价的 gold 基线，
# 丢了就没有比对基准；reports/.upstream-canary-pin.json —— 钉点唯一真源。
reports/canary/*.md
reports/dist-boundary-*.md
reports/upstream-intent-*.md
reports/upstream-watch-*.md
reports/.canary-state.json
