# S2.4 接力报告 · 2026-09-23（MiMo 会话卡顿收尾）

> 状态：**S2.4 已拍板、未动码**。下一刀从本文 §3 开干。
> 上一会话产出：S2.2 / S2.3 / Hermes 制度补丁三笔已合 main。

---

## 1. 工作空间（接棒先核）

| 项 | 值 |
|---|---|
| 分支 | `main` |
| HEAD | `950ea7a5ae`（chore: Hermes 反馈落地 — core 区登记 + G1 绿色边界） |
| 相对 origin/main | **ahead 3**，未 push：`66c732210c`（S2.2）→ `bacaacabd6`（S2.3）→ `950ea7a5ae`（Hermes） |
| 未夹带脏文件 | `tools/feedback_tool.py`（M，他人/前会话 WIP）—— **继续勿夹带** |
| 清理 | `.ff.txt` / `.ff2.txt` 已删 |

自测基线（本机 `.venv`，已复现多次）：
- `PYTHONPATH=. .venv/bin/python scripts/s2_snapshot.py --check` → 17 场景 × 3 段逐字通过
- S2 契约测：s22 8 + s23 20 + core_ledger 4 全绿；宽套件曾 111 passed

---

## 2. 已完成（勿重做）

| 切片 | commit | 要点 |
|---|---|---|
| S2.2 identity walking skeleton | `66c732210c` | `_resolve_section(name) → (content, source, content_hash)`；只迁 identity |
| S2.3 迁 15 注入点 + editing_guardrails YAML | `bacaacabd6` | 14 调用点全改 `_resolve_section(...)[0]`；YAML 408B 与常量字节等价；单次 `load_all_processors` 查找 |
| Hermes 四条 | `950ea7a5ae` | §7d `CORE_DIVERGE_LEDGER` C-001~C-008（8 个 core 文件，含 acp_adapter/server.py）；G1 green **只覆盖 follow**；`core登记率` 进 boundary；DISABLE 措辞改为「P3 计划项未落地」 |

交付报告：
- `reports/qclaw/s22-identity-walking-skeleton_20260923.md`
- `reports/qclaw/s23-migrate-sections_20260923.md`

---

## 3. 下一刀 = S2.4（**用户已拍板**）

### 拍板（勿再问）

1. **先开 S2.4**（不是 P3 `VERMES_DISABLE_PROMPT_SECTIONS`）
2. 策略：**以 YAML 为准回写常量，再删 `_PROCESSOR_FALLBACK`**
   - YAML 是 gold 真源；先把 4 键常量回写成 YAML 内容消双源
   - 然后删除整张 map
   - YAML 缺失 → `("", "missing" 警告)` 路径（保留 warning，不静默）
   - **`computer_use` 保留惰性分支**（工单 §9b.1 哨兵坑）

### 必读

- `docs/plans/2026-09-23-s2-pluginization-workorder.md` §5 S2.4 / **§9b.1**
- `tests/tools/test_s23_migrate_sections.py` 的 `KNOWN_YAML_CONSTANT_DRIFT`

### 实施步骤（建议顺序）

1. 建分支 `feat/s24-retire-fallback`
2. **回写 4 键漂移**（以 builtin YAML 为准改 `agent/prompt_builder.py` 常量）：
   - `task_completion`
   - `scholarforge_workflow`
   - `tool_use_enforcement`
   - `openai_model`
   - 改完 `KNOWN_YAML_CONSTANT_DRIFT` 应变空集或直接删该钉扎测
3. **退役 map**（`agent/system_prompt.py` `_PROCESSOR_FALLBACK`）：
   - 删 14 个字符串常量映射
   - `computer_use`：按 §9b.1 二选一——推荐 **保留显式惰性分支**（现有 `if name == "computer_use"` 已是），契约测钉住「删 YAML 后仍有兜底」
   - 缺失键路径保持 `""` + `logger.warning`
4. **改受影响测试**（已扫过，勿漏）：
   - `tests/tools/test_s21_register_section.py:203-208`（computer_use 哨兵断言 map 键 → 改断言惰性分支）
   - `tests/tools/test_s22_identity.py`（`_PROCESSOR_FALLBACK["identity"]`、map keys=15、fallback 路径测）
   - `tests/tools/test_s23_migrate_sections.py`（`set(CONSTANTS)==set(_PROCESSOR_FALLBACK)`、漂移表）
   - `tests/agent/test_w_l4_l5_m7_threshold.py:22`（`assertIs(sp._PROCESSOR_FALLBACK.get("editing_guardrails"), …)` → 改断言 YAML/常量等价）
5. **门**（硬）：
   - `s2_snapshot.py --check` 17×3 逐字绿（**不改注入文本**）
   - cache 哨兵 `test_stable_sha_stable_across_two_builds` 绿
   - 双探针：hide YAML → computer_use 仍 `fallback-lazy`；改 identity.yaml → gold 红 → 还原绿
6. 交付报告 `reports/qclaw/s24-retire-fallback_20260923.md` + 工单 §5 状态改 ✅
7. 只 add 本批文件（仍勿夹带 `feedback_tool.py`）

### 通过门（工单原文）

> 全场景绿 + cache 哨兵绿

---

## 4. 接棒卫生（重要）

**task 列表被上一会话工具回环污染**：存在大量重复子任务 `T5.1`…`T5.351+`（同名三条套娃）。请：
- `task abandon` 掉 T5 下全部重复项，只留干净的 3 步
- 或整组 abandon T5 后重建

根因：工具调用回环（同一 create/start/grep 重复数百次）。勿在新会话重放该模式——每步只调一次工具。

---

## 5. 之后排队（勿抢跑）

| 优先级 | 项 | 备注 |
|---|---|---|
| 下下刀 | P3 `VERMES_DISABLE_PROMPT_SECTIONS=id1,id2` | `register_plugin_processor` 前置过滤 + 禁用时**显式打印**；可发现性 `list_prompt_sections` 已在 |
| 可选 | push main（ahead 3） | `git -c http.version=HTTP/1.1 push`（SSL 偶发） |
| 可选 | `tools/feedback_tool.py` 单独收 | 与 S 系列无关 |

---

## 6. 纪律（沿用）

- gold 门闩先于一切；**S2.x 不改注入文本、不改 gold**
- 只 add 本批文件；数字带当场命令 + HEAD hash
- 双探针（应命中 + 应不命中）后再下结论
- 多关键词搜索用 Grep 工具或 `/usr/bin/grep`（WorkBuddy grep shim 假阴性）
- core 改动进 §7d `CORE_DIVERGE_LEDGER`（C-xxx）
- 每步一个分支、测过再合 main

— 完 —
