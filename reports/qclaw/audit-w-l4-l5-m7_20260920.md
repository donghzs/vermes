# P2 W-L4/L5/M7 落地交叉审计（QClaw）· 2026-09-20

> 审计对象：commit `60654e0aac`（feat(w-l4/l5/m7)）与 `ad58268ead`（web_dist）。
> 方法：读源码逐条核验，不采信 commit message 自述。

## 结论：通过，无返工

## 逐项核验

### W-L4（编辑护栏）
- 定义：`agent/prompt_builder.py:421 EDITING_GUARDRAILS_GUIDANCE`
- 三条齐全：`path:line` / 禁顺手重构 / 默认不 commit·push
- 注入：`system_prompt.py:63` `_PROCESSOR_FALLBACK["editing_guardrails"]` + `:244` 与 task_completion 同条件
- 硬闸：未动 `tools/approval.py`（仅提示层）✅ 对齐 QClaw「硬闸后置」保留

### W-L5（工作区事实块）
- 实现：`agent/workspace_facts.py`（207 行）
- 判据：`build_workspace_block` 返回空当无 git 区/探测失败 → **「有 git 区就注入」非渠道** ✅
- cwd 决策：`resolve_workspace_cwd`：TERMINAL_CWD 优先 → 交互式用进程 cwd → messaging 无 cwd `SKIP` ✅ 兜底对齐
- 注入层级：context tier（`system_prompt.py:466-473`），非 stable_parts ✅

### M7（token 阈值降级）
- `resolve_compact_skill_categories`：`auto` → `estimate_skills_index_bytes() >= skill_index_compact_threshold_bytes`（默认 20000）；`on` 始终；`off` 默认
- 与 platform/cwd **解耦** ✅（董董「按 prompt 长度不按场景」落地）

## 测试
- 技能索引相关 4 文件 **35 passed**
- 全量 `tests/agent/` 91 failed / 4542 passed：失败文件均不在本 commit 9 文件清单内，pre-existing 环境性失败（transports 网络/mcp 依赖），基线 92→91

## 非阻塞观察（不阻塞，不动手）
1. `estimate_skills_index_bytes()` 每次全量遍历 skills 目录；auto 默认 off + fail-safe，可接受；若 auto 常开建议缓存估算值。
2. `_INTERACTIVE_CODING_PLATFORMS`/`_HIDDEN_NOTE` 在 resolve 内仍保留但 auto 已不走平台白名单；docstring 注明保留给非 auto 用途，属干净遗留非死代码。

— QClaw 审计 · 2026-09-20
